@model app_asistencia_saferisk.Models.ReporteJornadaViewModel
@{
    ViewData["Title"] = "Reporte de Jornadas";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!--datatable css-->
<link rel="stylesheet" href="https://cdn.datatables.net/1.11.5/css/dataTables.bootstrap5.min.css" />
<!--datatable responsive css-->
<link rel="stylesheet" href="https://cdn.datatables.net/responsive/2.2.9/css/responsive.bootstrap.min.css" />

<link rel="stylesheet" href="https://cdn.datatables.net/buttons/2.2.2/css/buttons.dataTables.min.css">

<div class="container-xxl mt-4">
    <div class="card shadow border-0">
        <div class="card-header bg-primary-subtle d-flex justify-content-between align-items-center py-3 sticky-top" style="z-index:9">
            <h4 class="mb-0 text-primary">
                <i class="mdi mdi-clipboard-text-outline align-middle me-2"></i> @ViewData["Title"]
            </h4>
            <span class="badge bg-info-subtle text-info fs-6">
                Total: <b>@Model?.Detalles.Count</b>
            </span>
            <button class="btn btn-outline-primary btn-sm" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasFiltros" aria-controls="offcanvasFiltros" aria-label="Abrir filtros">
                <i class="mdi mdi-filter-variant"></i> Filtros
            </button>
        </div>
        <div class="card-body pb-2">

            <table id="tblJornadas" class="table nowrap dt-responsive align-middle table-hover table-bordered" style="width:100%">
                <caption class="visually-hidden">Reporte de jornadas laborales</caption>
                <thead class="table-light border-top">
                    <tr>
                        <th>Usuario</th>
                        <th>Fecha</th>
                        <th>Jornada</th>
                        <th>Entrada</th>
                        <th>Salida</th>
                        <th>Puntualidad</th>
                        <th>Horas Netas</th>
                        <th>% Cumplimiento</th>
                        <th>Semáforo</th>
                        <th>Atraso (min)</th>
                        <th>Almuerzo (min)</th>
                        <th>Break (min)</th>
                        <th>Permiso (min)</th>
                        <th>Traslado (min)</th>
                        <th>Estado</th>
                        <th>IP</th>
                        <th>GPS</th>
                        <th>Ubicación Oficina</th>
                        <th>Evento Actual</th>
                        <th>Observaciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model?.Detalles == null || !Model.Detalles.Any())
                    {
                        <tr>
                            <td colspan="20" class="text-center text-muted py-4">
                                <i class="mdi mdi-information-outline me-1"></i>
                                No existen jornadas en el rango seleccionado.
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>


@* Offcanvas de filtros *@
@await Html.PartialAsync("FiltrosJornadas")

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"
            integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>


    <!--datatable js-->
    <script src="https://cdn.datatables.net/1.11.5/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.5/js/dataTables.bootstrap5.min.js"></script>
    <script src="https://cdn.datatables.net/responsive/2.2.9/js/dataTables.responsive.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.2.2/js/dataTables.buttons.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.2.2/js/buttons.print.min.js"></script>
    <script src="https://cdn.datatables.net/buttons/2.2.2/js/buttons.html5.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.53/vfs_fonts.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.53/pdfmake.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/3.1.3/jszip.min.js"></script>

    <script src="~/assets/js/pages/datatables.init.js"></script>
    <script>
        $(function() {
          const table = $('#tblJornadas').DataTable({
            language: { url: "//cdn.datatables.net/plug-ins/1.13.7/i18n/es-ES.json" },
            pageLength: 10,
            responsive: true,
            order: [[1, 'desc']],
            dom: 'Bfrtip',
            buttons: [
              { extend: 'copy', className: 'btn btn-secondary btn-sm', text: '<i class="mdi mdi-content-copy"></i>' },
              { extend: 'csv', className: 'btn btn-success btn-sm', text: '<i class="mdi mdi-file-delimited-outline"></i>' },
              { extend: 'excel', className: 'btn btn-info btn-sm', text: '<i class="mdi mdi-file-excel-outline"></i>' },
              { extend: 'pdf', className: 'btn btn-danger btn-sm', text: '<i class="mdi mdi-file-pdf-outline"></i>' },
              { extend: 'print', className: 'btn btn-primary btn-sm', text: '<i class="mdi mdi-printer"></i>' }
            ]
          });

          // Filtros por AJAX
          $('#formFiltrosJornadas').on('submit', function(e) {
            e.preventDefault();
            $.getJSON('@Url.Action("GetJornadas", "Reporte")', $(this).serialize(), function(data) {
              table.clear().rows.add(data).draw();
            });
            $('#offcanvasFiltros').offcanvas('hide');
          });

          // Anclar offcanvas
          const pin = $('#pinOffcanvas');
          const pinned = localStorage.getItem('pinJornadas') === 'true';
          pin.prop('checked', pinned);
          if (pinned) $('#offcanvasFiltros').offcanvas('show');
          pin.on('change', function() {
            localStorage.setItem('pinJornadas', $(this).is(':checked'));
          });
        });
    </script>
}